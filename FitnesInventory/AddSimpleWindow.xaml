<Window x:Class="FitnesInventory.AddSimpleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" x:Name="lblField1" Content="Название:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtField1" Margin="5"/>
        
        <Label Grid.Row="1" Grid.Column="0" x:Name="lblField2" Content="Описание:" Visibility="Collapsed"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtField2" Margin="5" Visibility="Collapsed"/>
        
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="5">
            <Button x:Name="btnSave" Content="Сохранить" Width="80" Margin="5" 
                    Click="btnSave_Click" Background="LightGreen"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="80" Margin="5" 
                    Click="btnCancel_Click" Background="LightPink"/>
        </StackPanel>
    </Grid>
</Window>