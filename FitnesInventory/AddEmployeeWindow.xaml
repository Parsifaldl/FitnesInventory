<Window x:Class="FitnesInventory.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление сотрудника" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Имя:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtFirstName" Margin="5"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Фамилия:"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtLastName" Margin="5"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Должность:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtPosition" Margin="5"/>
        
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="5">
            <Button x:Name="btnSave" Content="Сохранить" Width="80" Margin="5" 
                    Click="btnSave_Click" Background="LightGreen"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="80" Margin="5" 
                    Click="btnCancel_Click" Background="LightPink"/>
        </StackPanel>
    </Grid>
</Window>