<Window x:Class="FitnesInventory.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Фитнес Инвентарь" 
        Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Фитнес Инвентарь - Учет" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"
                   Grid.Row="0"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,5">
            <StackPanel>
                <TextBlock Text="Логин" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameBox" Height="30" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль" FontWeight="Bold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="30" FontSize="14" Margin="0,0,0,15"/>

                <Button x:Name="LoginBtn" Content="Войти" 
                        Height="35" FontSize="14" FontWeight="Bold"
                        Background="LightGreen" Click="LoginBtn_Click"/>
            </StackPanel>
        </Border>

        <TextBlock x:Name="ErrorText" Grid.Row="3" 
                   Foreground="Red" TextAlignment="Center"
                   Margin="0,10" Visibility="Collapsed"/>
    </Grid>
</Window>